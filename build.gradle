apply plugin: 'com.android.application'

android {
    namespace "com.fagundes.multimarcas"
    compileSdkVersion 33
    defaultConfig {
        applicationId "com.fagundes.multimarcas"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}
